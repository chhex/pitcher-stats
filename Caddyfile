pitcher.watch, www.pitcher.watch {
    root * /srv/build

    handle /api/* {
        uri strip_prefix /api
        reverse_proxy api:8000
    }

    handle {
        file_server
        try_files {path} {path}/ /index.html
    }

    encode gzip
}
