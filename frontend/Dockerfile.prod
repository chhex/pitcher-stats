# Stage 1: Build
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

# Stage 2: Production (nur die gebauten Statischen Dateien)
FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/build /app/build
